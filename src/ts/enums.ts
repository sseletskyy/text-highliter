export enum Color {
    RED = 'red',
    GREEN = 'green',
    YELLOW = 'yellow',
    EMPTY = ''
}

export enum HighlightTextAreaMode {
    READ = 'READ',
    EDIT = 'EDIT'
}

export enum FilterPanelSortBy {
    BY_NAME = 'BY_NAME',
    BY_APPEARANCE = 'BY_APPEARANCE'
}
